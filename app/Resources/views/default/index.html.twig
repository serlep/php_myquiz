{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
      <!-- Example row of columns -->
      <div class="row">
        {% for category in categorys %}
          <div class="col-md-4">
            <a href="/{{category.name}}">
              <div class="jumbotron">
                <h2><center>{{category.name}}</center</h2>
              </div>
            </a>
          </div>
        {% endfor %}
      </div>
{% endblock %}


